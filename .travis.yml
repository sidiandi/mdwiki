language: node_js

node_js:
  - "0.10.12"

before_script:
  - npm install -g mocha
  - npm install -g bower
  - npm install -g karma
  - npm install
  - bower install
  - node app.js &
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start

script:
  - mocha
  - 'karma start --single-run --browsers PhantomJS'
